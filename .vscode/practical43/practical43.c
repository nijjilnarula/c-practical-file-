#include <stdio.h>
int main()
{
    int n,max,min ;
    max = 0;
    min = 0;
    printf("enter the total no inside the arr\n");
    scanf("%d", &n);
    int i, a[n];
    printf("enter the nos inside the array\n");
    for (i = 0; i <n; ++i)
    {
        scanf("%d", &a[i]);
    }
    for (i = 0; i < n; ++i)
    {
        if 
    }
    printf("the sum is %d\n", sum);
    avg = sum / n;
    printf("the avg is %d\n", avg);
}